[project]
name = "eso-underexplored"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
analysis = [
  "astropy>=7,<8",
  "matplotlib>=3.10,<4",
  "numpy>=2,<3",
  "scipy>=1.16,<2",
]

[tool.uv]
package = false
